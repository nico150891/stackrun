import { describe, it, expect } from 'vitest';

describe('CLI Integration', () => {
  it.todo('should display help when called with --help');
  it.todo('should display version when called with --version');
  it.todo('should show error for unknown commands');
});
