// Validator service — validates tool manifests against the schema
// Used by: install (before saving), call (before executing)
// Rules: see .claude/rules/manifest-validation.md
// TODO: implement in Phase 2
export {};
